<block wx:if="{{$taroCompReady}}">
    <view class="user-box">
        <at-message></at-message>
        <logged-mine></logged-mine>
        <block wx:if="{{!isLogged}}">
            <view class="login-button-box">
                <weapp-login-button></weapp-login-button>
            </view>
        </block>
    </view>
</block>