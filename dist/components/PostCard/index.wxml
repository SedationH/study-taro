<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp}}" bindtap="anonymousFunc0">
        <view class="post-title">{{title}}</view>
        <view class="post-content">{{content}}</view>
    </view>
</block>