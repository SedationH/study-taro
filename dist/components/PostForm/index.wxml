<block wx:if="{{$taroCompReady}}">
    <view class="post-form">
        <view>添加新的帖子
            <form bindsubmit="funPrivatebbzzz">
                <view>
                    <view class="form hint">标题</view>
                    <input class="input-title" type="text" placeholder="请输入标题" value="{{formTitle}}" bindinput="funPrivatebczzz" />
                    <view class="form-hint">正文</view>
                    <textarea placeholder="点击输入正文" class="input-content" value="{{formContent}}" bindinput="funPrivatebdzzz"></textarea>
                    <button class="form-button" form-type="submit" type="primary">提交</button>
                </view>
            </form>
        </view>
    </view>
</block>