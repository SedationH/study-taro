<block wx:if="{{$taroCompReady}}">
    <view class="post-form">
        <form bindsubmit="funPrivateizzzz">
            <view>
                <view class="form hint">标题</view>
                <input class="input-title" type="text" placeholder="请输入标题" value="{{formTitle}}" bindinput="funPrivatejzzzz" />
                <view class="form-hint">正文</view>
                <textarea placeholder="点击输入正文" class="input-content" value="{{formContent}}" bindinput="funPrivatebazzz"></textarea>
                <at-button compid="{{$compid__14}}">提交</at-button>
            </view>
        </form>
    </view>
</block>