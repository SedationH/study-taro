<block wx:if="{{$taroCompReady}}">
    <view class="logged-mine">
        <image src="{{avatar}}" class="mine-avatar" bindtap="anonymousFunc0"></image>
        <view class="mine-nickName">{{nickName}}</view>
    </view>
</block>